const listOfNumbers = [2, 4, 5, 2, 6]; // Initialize an array of numbers.

// Iterate through each element in the array and log it to the console.
listOfNumbers.forEach((element) => {
	console.log(element); // Log the current element.
});
