# Database Configuration
# PostgreSQL database credentials
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lyfter
DB_SCHEMA=lyfter_backend_project

# JWT Configuration
# IMPORTANT: Change this secret key in production!
JWT_SECRET_KEY=your-very-secure-secret-key-here-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Instructions:
# 1. Copy this file to .env (in the same directory)
# 2. Replace 'your_password_here' with your actual PostgreSQL password
# 3. Replace JWT_SECRET_KEY with a strong random key (use: python -c "import secrets; print(secrets.token_hex(32))")
# 4. NEVER commit the .env file to git!
